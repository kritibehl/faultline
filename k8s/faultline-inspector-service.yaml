apiVersion: v1
kind: Service
metadata:
  name: faultline-inspector
  labels:
    app: faultline-inspector
spec:
  type: ClusterIP
  selector:
    app: faultline-inspector
  ports:
    - name: http
      port: 8088
      targetPort: 8088
