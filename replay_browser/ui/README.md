# Faultline Replay Browser UI

Static browser for reviewing:

- replay search results
- lease timeline comparison
- stale-worker rejection path
- trace export phases
- duplicate-risk summary

Open locally:

```bash
open replay_browser/ui/index.html
