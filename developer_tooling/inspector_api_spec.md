# Inspector API Spec

## Endpoints

- GET /jobs/{id}
- GET /workers/{id}
- GET /leases/{id}
- GET /debug/retries
- GET /debug/duplicates

## Purpose
Expose runtime state for debugging worker coordination and lease behavior.
