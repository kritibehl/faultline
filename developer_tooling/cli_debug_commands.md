# CLI Debug Commands

```bash
faultline inspect job <job_id>
faultline inspect worker <worker_id>
faultline trace lease <lease_id>
faultline retries --job <job_id>
faultline duplicates --job <job_id>
